{
  "scripts": {
    "postinstall": "cd backend && npm install",
    "build": "npx webpack --mode production && cd backend && npm run build",
    "start": "node backend/dist/server.js"
  },
  "devDependencies": {
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
