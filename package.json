{
  "scripts": {
    "start": "node backend/dist/server.js",
    "build": "npx webpack",
    "postinstall": "cd backend && npm install && npm run build"
  },
  "devDependencies": {
    "webpack-cli": "^6.0.1"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
